<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.example.repository.HashtagMapper">

    <select id="selectHashtag" parameterType="Long" resultMap="HashtagResultMap">
        SELECT *
        FROM hashtag
        WHERE id = #{id}
    </select>

    <insert id="createHashtag" parameterType="org.example.domain.Hashtag">
        INSERT INTO hashtag
            (content)
        VALUES (#{content});
    </insert>

    <update id="updateHashtag" parameterType="org.example.domain.Hashtag">
        UPDATE hashtag set
            content = #{content},
        where id = #{id}
    </update>

    <delete id="deleteHashtag" parameterType="Long">
        DELETE from hashtag
        where id = #{id}
    </delete>

    <resultMap id="hashtagResultMap" type="org.example.domain.Hashtag">
        <id column="id" property="id" />
        <result column="content" property="content" />
    </resultMap>
</mapper>
